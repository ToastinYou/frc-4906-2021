package frc.robot.subsystems;

import edu.wpi.first.wpilibj.*;
import edu.wpi.first.wpilibj.DoubleSolenoid.Value;

import org.junit.Test;
//import static org.mockito.Mockito.*;

public class AirTest {
    /*@Test
    public void itSet() {
        Compressor compressorMock = mock(Compressor.class);
        DoubleSolenoid solenoidMock = mock(DoubleSolenoid.class);
        Air air = new Air(compressorMock, solenoidMock);

        air.getCompressorStatus();

        verify(compressorMock, times(2)).enabled();
    }*/
}